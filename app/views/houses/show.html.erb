<%= link_to "Back", :back %>
<h2><%= @house.name %></h2><%= link_to "Edit House", edit_house_path(@house) %> <%= link_to "Delete House", house_path(@house), method: :delete, data: {confirm: "Are you sure?"} %>
<h4>"<%= @house.motto %>"</h4>
<hr>
<% @house.characters.each do |character| %>
<%= character.name %>:
<%= character.title %>
<p>
    <% end %>

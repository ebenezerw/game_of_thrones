<%= link_to "Back", :back %>
<div><h2><%= @house.name %></h2><%= link_to "Edit House", edit_house_path(@house) %> | <%= link_to "Delete House", house_path(@house), method: :delete, data: {confirm: "Are you sure?"} %> | <%= link_to "Create New Character", new_house_character_path(@house) %>
<h4>"<%= @house.motto %>"</h4>
<hr>
<% @house.characters.each do |character| %>
<%= character.name %>:
<%= character.title %> | <%= link_to "edit", edit_house_character_path(@house, character) %> | <%= link_to 'delete', [character.house, character], method: :delete, data: {confirm: "Are you sure?"} %>
<p></div>
    <% end %>
